# run Dockerfile.database for local development

```bash
docker-compose -f docker-compose.local.yml up
```

# run Dockerfile.server for local development

```bash
docker-compose -f docker-compose.local.yml up
```
